<?xml version="1.0" encoding="utf-8"?>

<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/ic_bg_my_friend">

    <!-- ================= HEADER ================= -->

    <include
        android:id="@+id/header"
        layout="@layout/include_my_friend_header"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>


    <!-- =============== CONTENT ================= -->

    <FrameLayout
        android:id="@+id/contentContainer"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:animateLayoutChanges="true"
        app:layout_constraintTop_toBottomOf="@id/header"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- No Friend -->

        <include
            android:id="@+id/layoutNoFriend"
            layout="@layout/layout_no_friend"
            android:visibility="visible"/>

        <!-- Friend List -->

        <include
            android:id="@+id/layoutFriendList"
            layout="@layout/layout_friend_list"
            android:visibility="gone"/>

    </FrameLayout>


    <!-- ================= DIM ================= -->

    <View
        android:id="@+id/viewDim"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:background="#80000000"
        android:clickable="true"
        android:focusable="true"
        android:visibility="gone"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <!-- Friend Profile -->

        <include
            android:id="@+id/layoutFriendProfile"
            layout="@layout/fragment_friend_profile"
            android:visibility="gone"/>

        <!-- Remove Friend Dialog -->
        <include
            android:id="@+id/viewRemoveDialog"
            layout="@layout/dialog_remove_friend"
            android:visibility="gone"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"/>

        <!-- Success Dialog -->
        <include
            android:id="@+id/viewSuccessDialog"
            layout="@layout/dialog_success"
            android:visibility="gone"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="center"/>

    </FrameLayout>


</androidx.constraintlayout.widget.ConstraintLayout>